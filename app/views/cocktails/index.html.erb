<h1>Cocktails</h1>

<% @cocktails.each do |cocktail| %> <!--各カクテルのデータにアクセスするために、each メソッドを使用 -->
  <div>
    <h3><%= cocktail["cocktail_name"] %></h3>
    <p>Alcohol: <%= cocktail["alcohol"] %> %</p>
    <!-- ここで他のカクテル情報を表示することができます,例えば、説明や成分など -->
  </div>
<% end %>
